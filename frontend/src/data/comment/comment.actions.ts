import React from "react";
import { createCommentAsync, updateCommentAsync, deleteCommentAsync } from "./service/commentService";
import { AppThunk } from "../store";

export const actionCreateComment = (): AppThunk => (dispatch: React.Dispatch<any>, getState: any) => {

}

export const actionUpdateComment = (): AppThunk => (dispatch: React.Dispatch<any>, getState: any) => {

}

export const actionDeleteComment = (): AppThunk => (dispatch: React.Dispatch<any>, getState: any) => {

}

